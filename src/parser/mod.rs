//! The parser for the jsonpath.
//! The module grammar denotes the structure of the parsing grammar

use crate::parser::model::JsonPath;

//!The module model describes the structure of the parsed data
pub(crate) mod model;
//! The module parser denotes the mechanisms of parsing
pub(crate) mod parser;

